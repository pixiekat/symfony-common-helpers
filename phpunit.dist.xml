<?xml version="1.0" encoding="UTF-8"?>
<!--
  Lets the bundle be tested on its own, rather than only from inside a host
  application. That matters more once the package is published: a consumer (or
  a CI run) should be able to clone, `composer install`, and `vendor/bin/phpunit`
  without needing a Symfony app around it.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnWarning="true"
         failOnNotice="true"
         cacheDirectory=".phpunit.cache">
    <testsuites>
        <testsuite name="Pixiekat Symfony Helpers">
            <directory>tests</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
